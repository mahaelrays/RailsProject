<%= form_for(mygroup) do |f| %>
  <% if mygroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mygroup.errors.count, "error") %> prohibited this mygroup from being saved:</h2>

      <ul>
      <% mygroup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
