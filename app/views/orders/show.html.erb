<div class="bodyc">
<p id="notice"><%= notice %></p>

<table class="table table-striped table-bordered">
  <tr>
    <th >Name</th>
    <th >order</th>
    <th >restrant</th>
    <th >Menu</th>
  </tr>
  <tr>
    <td ><%= @order.user.name%></td>
    <td><%= @order.orderT%></td>
    <td><%= @order.resName%></td>
    <td ><%= image_tag @order.image.url(:thumb) %></td>
  </tr>
</table>







<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
